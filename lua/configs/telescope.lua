local options = {
  defaults = {
    layout_strategy = "vertical",
    layout_config = {
      preview_cutoff = 0,
      vertical = {
        height = 0.95,
      },
    },
    -- path_display = { "smart" },
  },
  extensions_list = { "themes", "terms", "live_grep_args" },
}

return options
